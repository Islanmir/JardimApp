name: Expo EAS Preview

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  preview:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: JardimApp   # 👈 nome da tua pasta

    steps:
      - name: 📦 Checkout código
        uses: actions/checkout@v4

      - name: ⚙️ Instalar Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: 📲 Instalar EAS CLI e dependências
        run: |
          npm install -g eas-cli
          npm install

      - name: 🚀 Publicar preview com EAS Update
        env:
          EXPO_TOKEN: ${{ secrets.EXPO_TOKEN }}
          CI: true
        run: |
          echo "🔑 Autenticar com token..."
          eas whoami || echo "Usar token armazenado no GitHub Secrets"
          echo "🚀 A enviar update para canal preview..."
          eas update --auto --branch preview --message "Atualização automática via GitHub Actions"



