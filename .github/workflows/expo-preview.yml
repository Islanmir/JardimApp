name: Expo EAS Preview

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  preview:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: JardimApp   # 👈 altera se a tua pasta tiver outro nome

    steps:
      - name: 📦 Checkout código
        uses: actions/checkout@v4

      - name: ⚙️ Instalar Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: 📲 Instalar EAS CLI e dependências
        run: |
          npm install -g eas-cli
          npm install

      - name: 🚀 Publicar preview Expo (EAS)
        env:
          EXPO_TOKEN: ${{ secrets.EXPO_TOKEN }}
          CI: true
        run: |
          eas whoami || echo "A autenticar com token..."
          eas update --auto --branch preview --message "Atualização automática"


